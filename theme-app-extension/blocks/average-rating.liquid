{% assign avg_rating = block.settings.product.metafields.prapp.avg_rating | plus: 0 %}

{% if avg_rating > 0 %}
<div class="prapp-average-block" style="padding: {{ block.settings.padding_y }}px {{ block.settings.padding_x }}px;" data-product-id="{{ block.settings.product.id }}">
  {% if block.settings.show_heading and block.settings.heading != blank %}
    <h4>{{ block.settings.heading }}</h4>
  {% endif %}
  {% render "star-rating", rating: avg_rating, fill: block.settings.stars_fill_color, size: block.settings.star_size %}
</div>
{% endif %}

{% schema %}
{
  "name": "Average Review Score",
  "templates": ["product"],
  "target": "section",
  "stylesheet": "star-rating.css",
  "settings": [
    {
      "type": "color",
      "id": "stars_fill_color",
      "label": "Stars color",
      "default": "#000000"
    },
    {
      "type": "number",
      "id": "star_size",
      "label": "Star size",
      "default": 24
    },
    {
      "type": "number",
      "id": "padding_x",
      "label": "Horizontal spacing",
      "default": 0
    },
    {
      "type": "number",
      "id": "padding_y",
      "label": "Vertical spacing",
      "default": 0
    },
    {
      "id": "show_heading",
      "type": "checkbox",
      "label": "Show Heading",
      "default": false
    },
    {
      "id": "heading",
      "type": "text",
      "label": "Heading",
      "default": "Average Score"
    },
    {
      "id": "product",
      "type": "product",
      "label": "Product",
      "autofill": true
    }
  ]
}
{% endschema %}
